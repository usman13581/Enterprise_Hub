# Copy to apps/web/.env.local before running the web app.

NEXT_PUBLIC_API_URL="http://localhost:3001"

# Must match BOOTSTRAP_TOKEN in apps/api/.env. This value ships in the browser
# bundle, so treat it as a development convenience, not a secret.
NEXT_PUBLIC_BOOTSTRAP_TOKEN="binhaj-dev-token"
